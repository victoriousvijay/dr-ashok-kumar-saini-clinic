# Public site URL, used for metadata, canonical URLs and structured data.
NEXT_PUBLIC_SITE_URL=https://www.example.com

# Supabase project credentials. Never commit real values.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Used exclusively by app/api/leads/route.ts to insert public
# enquiries. Never expose this to the client or commit it.
SUPABASE_SERVICE_ROLE_KEY=
